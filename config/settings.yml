mailer:
  default_url_options:
    host: '0.0.0.0:3000'
  # action mailer smtp setting
  smtp:
    address: '127.0.0.1'
    port: 25
  devise:
    sender: 'sender@domain'

devise:
  secret_key: 'default'
sidekiq:
  redis_url: 'redis://localhost/0'